
service:
  name: urlshortener
  compute: fargate

app:
  port: 3000
  healthPath: /api/health
  livePath: /api/live
  basePath: /
  user: urlshortener_app

deploy:
  env: dev
  containerUid: 10005

storage:
  volumes:
    - name: urlshortener-volume
      emptyDir: {}

  volumeMounts:
    - name: urlshortener-volume
      mountPath: /data

features:
  topVolume:
    enabled: true
  tmp:
    enabled: true


