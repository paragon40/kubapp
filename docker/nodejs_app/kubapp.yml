
service:
  name: nodejsapp

app:
  port: 3000
  healthPath: api/health

deploy:
  env: dev
  user: nodejs_app
  containerUid: 10004

runtime:
  env:
    NODE_ENV: developement
    REDIS_HOST: redis-service
    REDIS_PORT: "6379"
    LOG_LEVEL: info
