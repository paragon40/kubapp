# =====================================================
# IAM (IRSA + platform roles)
# =====================================================

AWS.IAM.PolicyWildcard
AWS.IAM.PolicyActionWildcard

# =====================================================
# NETWORK (intentional public subnet usage for ingress)
# =====================================================

AWS.VPC.SubnetPublicIP

# =====================================================
# LOGGING (CloudWatch access via IAM roles)
# =====================================================

AWS.CloudWatch.*
