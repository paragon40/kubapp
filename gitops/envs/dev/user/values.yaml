appName: user

namespace: dev

replicaCount: 2

image:
  repository: codest40/weather_app
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetPort: 80

resources:
  requests:
    cpu: 100m
  limits:
    cpu: 500m

env:
  RUN_MIGRATIONS: "true"
  USE_SQLITE: true
hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  cpu: 70

