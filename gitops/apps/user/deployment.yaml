apiVersion: apps/v1
kind: Deployment
metadata:
  name: user
  namespace: ingress
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user
  template:
    metadata:
      labels:
        app: user
    spec:
      containers:
        - name: user
          image: hashicorp/http-echo
          args:
            - "-text=USER Service"
          ports:
            - containerPort: 5678
