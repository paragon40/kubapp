# All Infra provision via Terraform

“KubApp is a production-style platform that implements a full GitOps workflow on Kubernetes, with strict separation between infrastructure, workload orchestration, and deployment automation including multi-environment isolation, workload identity (IRSA), GitOps delivery, and observability.”

“IRSA eliminates the need for long-lived AWS credentials inside pods, aligning with least-privilege and zero-trust principles.”
