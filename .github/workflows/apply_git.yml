name: GitOps Test

on:
  push: {}
  workflow_dispatch:

jobs:
  test-production-env:
    runs-on: ubuntu-latest
    environment: development

    steps:
      - name: Trigger gitops
        run: |
          ./scripts/apply_gitops.sh

