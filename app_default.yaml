service:
  name: default

app:
  port: 3000
  healthPath: /

deploy:
  env: dev
  replicas: 2

runtime:
  env:
    NODE_ENV: develeopment
    REDIS_HOST: redis-service
    LOG_LEVEL: info

  secretRefs:
    - default-secrets

